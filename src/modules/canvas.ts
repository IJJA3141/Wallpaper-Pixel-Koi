const ct: HTMLCanvasElement = <HTMLCanvasElement>(
  document.getElementById("batteryCanvasTop")
);
const cd: HTMLCanvasElement = <HTMLCanvasElement>(
  document.getElementById("batteryCanvasDown")
);

var ctx: CanvasRenderingContext2D = <CanvasRenderingContext2D>(
  cd.getContext("2d")
);

var ab: boolean[] = [
  true,
  true,
  true,
  true,
  true,
  true,
  true,
  true,
  true,
  true,
  true,
  true,
];

var aab: boolean[][];
for (var i = 0; i < 100; i++) {
    aab.
}
